# Postgres connection string (Neon/Render in production, local Postgres in dev)
DATABASE_URL=postgres://user:password@localhost:5432/rewind

# Auth.js — generate with: npx auth secret
AUTH_SECRET=

# GitHub OAuth App credentials
AUTH_GITHUB_ID=
AUTH_GITHUB_SECRET=
